export default function DARPANPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold">DARPAN</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        DARPAN ships in Phase 4. This route exists so the app shell&apos;s sidebar has
        somewhere to link, per Phase 0 task 10.
      </p>
    </div>
  );
}
