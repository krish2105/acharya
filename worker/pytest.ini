[pytest]
pythonpath = .
testpaths = tests
markers =
    requires_ollama: needs a local Ollama server with the pinned model pulled; excluded from CI (no GPU/multi-GB model on hosted runners), run locally with `pytest` (no -m filter).
